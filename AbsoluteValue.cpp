#include<iostream>
using namespace std;
int main()
{
    int n;
    
    cout<<"Enter a no : ";
    cin>> n;

    if(n>=0)
    {
        cout<<n<<endl;
    }
    else
    {
        cout<<-n;
    }

    //if u want to change into absolute value

    // if(n<0)
    // cout<<-n;
}